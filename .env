# This file exists for setting up an environment and for security reasons
# IMPORTANT use VITE_ as a pretag, than the name

# This stores the api url, wich can be used by the frontend, 
# without needing to go into the backend,
# and installing all the npm modules to the frontend as well
VITE_API_URL=http://localhost:3000
